- Used per-test dynamic module loaders instead of fixtures so missing future files in `analysis/plot_opt_single_layer_prefill.py`, `scripts/run_opt_single_layer_prefill_profile.py`, and `python/nvbenchsuite/opt_single_layer_profile.py` collect cleanly and fail as RED-phase test failures rather than setup errors.
- Locked the contract with small local fixtures: mixed-unit duration rows for exact time-weighted ACU/GBU math, fake decoder-layer parameter enumeration for direct single-layer counts, ancestry tuples for NVTX bucket mapping, and an explicit 6.7B failure-manifest row with fallback fields pinned off.
- For the sliced runtime core, the safe pattern is: fetch only OPT config metadata, instantiate a single `OPTDecoderLayer`, then load just `model.decoder.layers.{layer_index}.*` tensors from the checkpoint index / shard files instead of constructing any full-model module tree.
- In the working standalone-layer runtime (`transformers` 5.3.0 here), `OPTDecoderLayer(..., use_cache=True)` returned a bare `tuple` of length 1 for the isolated prefill path, not an object with `hidden_states` / `last_hidden_state`; the output extractor needs to accept tuple/list/direct-tensor forms instead of assuming one structured wrapper.
